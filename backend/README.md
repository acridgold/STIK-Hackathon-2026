## Установка
1. Клонируй репозиторий
2. Скопируй файл с переменными окружения:
   cp .env.example .env
3. Заполни .env своими значениями

4. python -m venv venv

5. source venv/bin/activate 

6. Установи зависимости:
   pip install -r requirements.txt

7. Запусти проект:
   python app.py